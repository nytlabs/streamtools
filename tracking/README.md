STREAMTOOLS - Tracking
====================

Maintains state, which is continuously updated on each input message. No output (forgettable).

* `type-inferencer` - outputs flattened keys of input blob, and corresponding JSON type.
