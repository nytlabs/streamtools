STREAMTOOLS - Transform
====================

Takes incoming stream and produces a different stream of output; single-in-single-out.

* `type-inferencer` - outputs flattened keys of input blob, and corresponding JSON type.